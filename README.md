# win-nw-bandwidth-logger
A PowerShell script that logs per-interface network bandwidth (sent, received, and total) in Mbps on Windows. It stores data in daily folders with separate CSV files for each network interface. Useful for tracking bandwidth usage over time in a clean, automated way.
